import { PageShell } from "@/components/layout/PageShell";
import { WorkbenchAdmin } from "@/components/workbench/WorkbenchAdmin";

export default function WorkbenchAdminPage() {
  return (
    <PageShell>
      <WorkbenchAdmin />
    </PageShell>
  );
}
