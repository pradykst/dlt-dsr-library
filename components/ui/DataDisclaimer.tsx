export function DataDisclaimer() {
  return (
    <div className="mt-4 max-w-3xl border border-line bg-paper px-4 py-3 text-sm leading-6 text-muted">
      This view uses a limited, moderately accurate seeded dataset. Treat it as a preview of how the design knowledge flow may look after peer review, not as a finalized evidence base.
    </div>
  );
}
